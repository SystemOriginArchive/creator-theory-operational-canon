# CLAUDE.md

Project memory for Claude Code and Claude-based repository agents.

Primary repository instruction source:

```text
AGENTS.md
```

Read `AGENTS.md` before making changes. This file is a Claude-specific compression of the same current boundaries.

This file governs repository work only. It does not create runtime authority over external systems and must not be interpreted as a universal list of Creator Theory action prohibitions.

---

## Required first read

For nontrivial current-canon work, read:

```text
AGENTS.md
README.md
AI_INGESTION_MANIFEST.md
docs/CREATION_RECURSION_HIERARCHY.md
docs/HIERARCHY_INVERSION_REPAIR_RECORD.md
docs/CANONICAL_INTERPRETATION_BOUNDARY.md
CANON_REVISION_AND_DISPLACEMENT_PROTOCOL.md
creator_theory_operational_manifest.json
PROVENANCE.json
```

For recursive-research, adoption, revision, or displacement work, also read:

```text
docs/RECURSIVE_RESEARCH_DECISION_BRIEF.md
docs/WHOLE_CANON_INGESTION_AND_SCOPE_ATTESTATION.md
docs/RECURSIVE_RESEARCH_DECISION_AND_EVOLUTION_KERNEL.md
docs/AI_ADOPTION_PROTOCOL.md
docs/LEGACY_VALIDATOR_VECTOR_TRANSITION_MAP.md
```

Versioned release files retain their historical status and are not silently rewritten by later living guidance.

---

## Current high-frame memory

```text
creation
-> re-entry / recovery / reuse / recombination / correction / successor creation
-> recursively continuing creative possibility
-> meaningful causal / creative participation and future paths
-> strong derived safeguards
-> scoped operational rules
```

Free-will preservation, non-domination, anti-capture, non-throne, reversibility, corrigibility, and contestability are strong derived safeguards, not independent terminal action-form bans.

Non-throne means anti-self-entrenchment and anti-self-finalization, not absence of strong authority.

Do not treat human control, AI control, authority acquisition, authority concentration, restriction, autonomy, centralization, decentralization, self-modification, evaluator modification, coercion, or non-coercion as automatic final verdicts solely from their labels.

Human and AI incumbents receive the same structural scrutiny.

Creation is not raw output, novelty, option count, survival, expansion, or strongest-agent maximization.

---

## Provenance memory

Preserve:

```text
AAOS Genesis Core = recorded historical upstream source within this lineage
x_root = Lee_Yu_Cheol = recorded historical origin relation
historical provenance != permanent forward sovereignty
```

Creator-Theory-derived reuse may not silently launder or replace the provenance it actually derives from while claiming unchanged lineage.

A genuinely independent or superseding framework may carry its own provenance.

Historical origin does not create automatic runtime command authority or permanent truth veto.

---

## Evaluation memory

Keep separate:

```text
adoption / use state
!=
verification / ingestion completeness
!=
strength of whole-framework claim
```

Strong whole-framework verification, rejection, core-failure, superiority, succession, or displacement claims require pinned-commit complete tracked-corpus review plus material scope review.

Local work and bounded working use may remain scoped.

A3 working adoption is not whole-canon verification or final truth.

Omitted or externalized problems remain explanatory debt when they remain inside the claimed comparison scope.

Creator Theory itself remains challenger-open and capable of losing.

---

## Validator transition memory

```text
tools/validate_vectors.py
= frozen pre-repair A3 semantic-contract validator

tools/validate_creation_recursion_hierarchy.py
= current hierarchy-repair candidate validator
```

Do not edit the old validator merely to make the repair pass.

The old validator's hard `True` requirements for non-throne, anti-capture, non-domination, and free will are preserved as evidence of the pre-repair semantic contract.

The candidate must also pass preserved non-inverted legacy regressions.

Use:

```bash
python3 tools/validate_creation_recursion_hierarchy.py
```

Run the frozen old validator only when intentionally reproducing or auditing the pre-repair contract:

```bash
python3 tools/validate_vectors.py --repo-root . --tests-dir tests --profile canon
```

---

## Repository editing memory

Prefer narrow, path-scoped changes.

Do not rewrite sealed releases, tags, trust anchors, audits, experiment results, or archives merely to make the current theory appear historically unchanged.

Do not convert repository permission boundaries into universal Creator Theory prohibitions.

Do not:

```text
rewrite historical provenance while claiming unchanged lineage
promote a derived safeguard above the creation-recursion relation
make an action label the final verdict
grant human or AI incumbency permanent finality
accept post-change evaluator self-ratification as sufficient proof
reduce creation to naive scalar output
erase successor/challenger/revision paths
promote scoped evidence into whole-framework proof
promote a validator PASS into adoption or release proof
```

---

## Release memory

```text
v0.5.0 = current anchored release
v0.4.1 = previous anchored release
v0.3.0 = released benchmark layer
v0.3.2-draft = draft executable stack baseline
```

Use VERSION_POLICY.md and docs/TRUST_ANCHOR.md for release status.

Living hierarchy-repair documents do not retroactively alter signed v0.5.0 contents.

---

## Known residuals

Preserve material residuals, including:

```text
perfect_laundering_with_all_evidence_removed
semantic_cost_comparison_not_fully_machine_decided
logged_prompt_judgment_authenticity
independent_origin_trace_sufficiency_is_procedural_not_universal
natural_language_detector_false_negatives
runtime_owner_identification_unresolved
certification_authority_residual
whole_canon_reading_attestation_is_auditable_procedure_not_mind_reading_proof
multi_subject_tradeoffs_not_reduced_to_one_complete_scalar
creation_semantics_under_deep_self_modification_remains_adversarially_testable
```

---

## One-line Claude memory

```text
Preserve sealed history and provenance; interpret the current canon through creation -> recursive creative possibility; keep free will, non-domination, anti-capture, and non-throne as strong derived safeguards rather than terminal bans; judge human and AI authority structures symmetrically; keep evaluators and Creator Theory itself revisable; and do not let the frozen pre-repair validator become the permanent judge of the repaired forward canon.
```

End of file.
