# Attribution Statement

## Status

INERT UNTIL USER-SIGNED.

This statement is not a release signature, not a key-generation record, and not executable authority.

## Statement Body

The controlling public key for a signed PROV-K release is attributed, at the documentation layer, to Lee_Yu_Cheol as origin attribution for this canon chain.

This attribution does not convert the key into a personal throne, arbitrary command authority, institutional ownership, capital capture, state capture, platform capture, or AI capture.

The key's role is to help verify provenance continuity for signed release manifests.

## Empty Signature Section

```text
ATTRIBUTION_SIGNATURE: 
SIGNED_UTC: 
PUBLIC_KEY_FINGERPRINT: 
```

## Offline Signing Instructions

1. Wait for an auditor to review the branch and any helper script line by line.
2. Generate or select the real origin key only outside the repository, using a user-controlled offline process.
3. Keep the private key outside this repository and outside any synced project folder.
4. Never paste, photograph, print, upload, or commit the private key.
5. Review this attribution statement before signing it.
6. Sign release manifests only after the audit pass, using a private-key path outside the repository.
7. Use `python3 -m tools.prov_k.cli sign --repo-root . --manifest <manifest-path> --private-key <outside-repo-private-key-path>` only after the user has created the real key outside the repo.

This document intentionally contains no private key and no completed signature.
