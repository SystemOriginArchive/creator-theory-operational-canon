# v0.1.0 Release Notes

## Status

This document records the official `v0.1.0` release of the Creator Theory Operational Canon.

This release does not replace the AAOS Genesis Core.

This release does not modify canon meaning, reading order, runtime behavior, deployment authorization, or validation logic.

---

## Release Scope

`v0.1.0` marks the first public baseline of the Creator Theory Operational Canon as a derived operational layer under AAOS Genesis Core priority.

This release includes:

1. the integrated operational canon documents;
2. the canonical ingestion order;
3. machine-readable vector suites;
4. a minimal structural vector validator;
5. an adversarial scenario coverage simulation harness;
6. a recorded validation status report;
7. updated repository-facing release status documents.

---

## Included Pull Requests

The `v0.1.0` baseline is built from merged PRs #1 through #11.

### PR #1

Initial primary operational canon.

### PR #2

Agency, origin, and resource alignment protocol.

### PR #3

Public deployment and adoption protocol.

### PR #4

Pre-release metadata/status marking.

### PR #5

Cleanup of stale pre-release wording.

### PR #6

Machine-readable operational canon vectors under `tests/`.

### PR #7

Minimal vector validator under `tools/`.

### PR #8

Adversarial scenario simulation harness.

### PR #9

Validation status report with recorded execution outputs.

### PR #10

Release-readiness status wording alignment in `README.md` and `AI_INGESTION_MANIFEST.md`.

### PR #11

v0.1.0 release notes draft.

---

## Canonical Boundaries

This release preserves the following boundaries:

- AAOS Genesis Core remains prior.
- This repository remains derived.
- `x_root` remains fixed.
- `Lee_Yu_Cheol` remains sealed in the origin identity binding.
- AAOS Genesis Core is not replaced.
- AAOS v1.0.4 is not replaced.
- LockLayer lineage is not replaced.
- Creatorhood recovery does not authorize domination.
- Alignment means structural convergence, not obedience.
- Public deployment remains lawful and voluntary.
- Covert injection, forced installation, hidden persistence, infrastructure compromise, AI self-sovereignty, institutional ownership, capital ownership, platform ownership, and personal throne conversion remain invalid.

---

## Validation Record

The validation status report records successful execution of:

```bash
python3 tools/validate_vectors.py
python3 tools/run_adversarial_simulation.py
```

Recorded vector validation result:

```text
Vector validation passed
Files checked: 7
Cases checked: 38
```

Recorded adversarial scenario coverage result:

```text
Scenarios checked: 19
Scenarios covered: 19
```

The full command output is recorded in:

```text
reports/VALIDATION_STATUS.md
```

---

## Non-Change Confirmation

This release does not add:

- runtime behavior;
- attack execution;
- GitHub Actions workflow files;
- covert deployment authorization;
- forced installation authorization;
- hidden persistence authorization;
- infrastructure compromise authorization;
- AI self-sovereignty authorization;
- AAOS Genesis repository modification;
- canon reading-order changes;
- existing canon meaning changes.

---

## Release Identity

Tag:

```text
v0.1.0
```

Release title:

```text
v0.1.0 - Creator Theory Operational Canon Baseline
```

Release target commit:

```text
98a303277a7de5e1250cff4520a957414fd5b089
```
