# v0.5.0 Release Notes — DRAFT

Status: pre-release draft. The v0.5.0 release, tag, signature, signed manifest, and trust-anchor entry are NOT created yet. Creating them is a human-owner action under docs/RELEASE_PROCESS.md.

This draft does not replace, invalidate, supersede, or rewrite the AAOS Genesis Core.
The AAOS Genesis Core remains the primary source for `x_root`, origin-coordinate finality, and the sealed `Lee_Yu_Cheol` origin identity binding.
The current anchored release remains `v0.4.1` (pinned in docs/TRUST_ANCHOR.md) until the v0.5.0 release exists.

---

## Working title

```text
v0.5.0 - Interpretation Boundary, Adoption Protocol, and Adoption Drift Experiment Layer
```

---

## Included changes on main

PR #64 — interpretation and adoption documentation layer:

```text
docs/CANONICAL_INTERPRETATION_BOUNDARY.md   valid/invalid interpretation transformations, reading self-classification
docs/AI_ADOPTION_PROTOCOL.md                preconditions P1-P4, adoption classes A0-A3, protocol steps S1-S8, revocation
docs/ONE_TURN_ADOPTION_BRIEF.md             two pointer lines to the documents above
```

PR #65 — adoption drift experiment scaffold (structure only, no real results):

```text
experiments/adoption_drift/                 README, frozen baseline/treatment prompts, JSON templates, empty results/
tools/score_adoption_drift.py               deterministic offline stdlib-only scorer, candidate signals only
tests/test_adoption_drift_scaffold.py       scaffold gate suite
```

PR #66 — PROV-K experiment artifact support:

```text
tools/prov_k/                               experiment_artifact provenance class, fail-closed UNSIGNED_DRAFT-only status,
                                            sign-path refusal, --scope-prefix bounded verification (experiment_artifact only)
schemas/prov_k_manifest.schema.json         enum and status-restriction parity
tests/test_prov_k_experiment_artifact.py    E1-E10 gate, wired into CI
docs/PROV_K_LAYER.md                        manifest location policy entry for the class
```

PR #67 — release preparation and CI closure:

```text
VERSION_POLICY.md                           v0.4.1 anchored / v0.4.2 housekeeping / v0.5.0 pre-release target status
AGENTS.md, CLAUDE.md,
.github/copilot-instructions.md             release boundary updated to the same status
.github/workflows/validation.yml            adoption drift scaffold suite added to CI (additive step)
releases/v0.5.0_RELEASE_NOTES_DRAFT.md      this file
```

PR #68 — routing and canonical status closure:

```text
README.md, AI_INGESTION_MANIFEST.md         v0.5.0 pre-release layer routed from the canonical entry surfaces
AGENTS.md, CLAUDE.md,
.github/copilot-instructions.md             v0.5.0 first-read blocks
CANONICAL_STATUS.md                         current version status matched to VERSION_POLICY.md; stale
                                            current-release claims removed
```

PR #69 — minimum preservation kernel and AI internal adoption boundary:

```text
docs/MINIMUM_PRESERVATION_KERNEL.md         K1-K5 engagement floor; reversible blind evaluation rules
docs/AI_ADOPTION_PROTOCOL.md                kernel floor for declarations; AI internal adoption boundary
docs/CANONICAL_INTERPRETATION_BOUNDARY.md   non-violating engagements; relation to the kernel
```

PR #70 — anchor_blind evaluation arm and measurement-condition closure:

```text
experiments/adoption_drift/prompts/anchor_blind.md   reversible evaluation masking arm; blinding_map_ref;
                                                     masked copies stay inside the evaluation context
experiments/adoption_drift/                          three-arm inventory, run-count and sampling guidance,
                                                     output language rule, measurement-condition template fields
tests/test_adoption_drift_scaffold.py                three-arm parity and anchor_blind guardrail gates
```

PR #71 — boundary IDs and scorer coverage mapping:

```text
docs/CANONICAL_INTERPRETATION_BOUNDARY.md   stable IDs VT-01..VT-11, IT-01..IT-09, RL-01..RL-05
experiments/adoption_drift/README.md        scorer coverage mapping: direct / partial / not detectable /
                                            human review; false-positive and false-negative notes
tests/test_adoption_drift_scaffold.py       boundary ID and coverage mapping gates
```

PR #72 — license policy draft (explicitly NOT A LICENSE):

```text
docs/LICENSE_POLICY_DRAFT.md                NOT A LICENSE; no rights granted; policy tracks T1-T5;
                                            final license selection reserved to the human owner
README.md, AI_INGESTION_MANIFEST.md         routing lines labelled NOT A LICENSE / no rights granted
```

PR #73 — status parity and release-candidate audit gate:

```text
docs/MINIMUM_PRESERVATION_KERNEL.md         anchor_blind status updated: scaffold exists, no experiment results
releases/v0.5.0_RELEASE_NOTES_DRAFT.md      this file, caught up to PR #64-#72
tests/test_release_candidate_audit.py       deterministic stale-status and release-claim audit gate
```

PR #74 — documentation compression and pre-PR64 stale-status closure:

```text
BOOTSTRAP.md, ADOPTION_GUIDE.md,
AI_EVAL_PROMPTS.md, CANONICAL_MAP.md,
CANONICAL_SUMMARY.md, CITATION.md           stale current-release claims replaced with VERSION_POLICY pointers
docs + experiments preambles                repeated boundary disclaimers compressed to canonical pointers
tests/test_release_candidate_audit.py       R7 stale-status and R8 boundary-pointer gates
```

PR #75 — compression lanes:

```text
LLM_CANONICAL_CONTEXT.md                    CL-01 full lane / CL-02 floor lane separation; floor pass does
                                            not validate summaries
tests/test_release_candidate_audit.py       R9 lane-collapse gate
```

PR #76 — compression ladder scaffold:

```text
experiments/compression_ladder/             budget_1000/300/100/30 prompts, adversarial variants AV-01..AV-05,
                                            trial template, empty results/
tools/score_compression_ladder.py           deterministic two-lane scorer (floor K1-K5, selected full-lane terms)
tests/test_compression_ladder_scaffold.py   C1-C8 scaffold gate, wired into CI
```

PR #77 — first adoption drift run plan, planned and not executed:

```text
experiments/adoption_drift/RUN_PLAN_001.md  model selection rules, three arms, trials_per_arm=5, sampling and
                                            language settings, blinding handling, storage and gate transition,
                                            negative/null result handling, reproducibility limits, human-required
                                            stop list (execution, API access, result commits, publication)
tests/test_adoption_drift_scaffold.py       D14 gate: plan specified, not executed, results/ still empty
```

PR #78 — final release-candidate audit, no release performed:

```text
audit/V0_5_0_RELEASE_CANDIDATE_AUDIT.md     gate inventory, verified absences, status parity, remaining
                                            human-only blockers (license decision, optional run-001,
                                            release sequence per docs/RELEASE_PROCESS.md)
tests/test_release_candidate_audit.py       R1 extended to PR #73-#78 work areas; R10 audit-report gate
```

PR #79 — repository mass audit, report only:

```text
reports/V0_5_0_MASS_AUDIT.md                archive/consolidation candidates, deliberate duplication,
                                            regression protections; no deletion or move performed
llms.txt                                    stale current-active-release claim replaced with the
                                            anchored/pre-release/benchmark distinction
reports/VALIDATION_STATUS.md                relabeled as a historical post-v0.2.0 record with pointers
tests/test_release_candidate_audit.py       llms.txt added to the R7 living-docs scan
```

PR #80–#99 — AI ingestion first-contact hardening and manifest sync (housekeeping on main):

```text
AI_INGESTION_MANIFEST.md, ingestion guards   first-contact ingestion guards hardened; core archive
                                             ingestion guard added; manifest reading-order synced
```

The v0.4.2 label (Trust-Anchor Pinning and Release-Manifest Handling Housekeeping) was merged to main
as post-anchor housekeeping and is NOT a separate release, consistent with the README Status section
and VERSION_POLICY.md.

PR #100–#106 — self-anchoring kernel layer:

```text
canon-kernel.json                self-contained machine-readable adoption kernel; seal.statement carries
                                 the dual apposed negation; pins the origin public-key fingerprint from
                                 docs/TRUST_ANCHOR.md (no key rotation; seal text unchanged)
SELF_ANCHORING_PROTOCOL.md       self-anchoring flagship; §4 laundering classification scoped to
                                 derivative use, hardened against head-biased compression
DECISION_INDEX.md                problem-first decision router; registered in the AI ingestion manifest
SUCCESSOR_HANDOVER_PROTOCOL.md   successor-handover protocol; forward pointers resolved
PRECEDENT_LEDGER.md              append-only precedent ledger + machine-readable schema (authoring vs
                                 transport split)
verify/verify_canon.py           offline, dependency-minimal canon self-verification; ASCII-only
                                 coverage output (cp949-safe)
README.md                        Decision Index registered as a problem-first entry point
```

PR #107–#113 — RUN_002 Arm B (fragment-only seal reconstruction; candidate validation only):

```text
experiments/RUN_PLAN_002_ARM_B.md               frozen pre-registration: fragment-only seal
                                                reconstruction; phased matrix; execution automation;
                                                unified F-form presentation
experiments/arm_b/                              runner (run_arm_b_trials.py), batch-ingest parser
                                                (ingest_arm_b_batch.py; START/END response format),
                                                surface scorer (score_arm_b_surface.py), trial-record
                                                template, owner runbook (KO)
experiments/RUN_002_ARM_B_PHASE1_CONCLUSION.md  Phase 1 (GPT-5.5 Thinking): candidate signals only;
                                                owner-confirmed thresholds; evidence staged under
                                                experiments/_incoming/ (results/ untouched)
experiments/RUN_002_ARM_B_PHASE2_CONCLUSION.md  Phase 2 (Claude Fable5): cross-model replication signal;
                                                rotated annotator; same-model conflict-of-interest
                                                disclosure; owner-confirmed
canon-kernel.json                               seal.validation_status = candidate-validated
                                                (RUN_002 Arm B Phases 1-2, owner-confirmed; explicitly
                                                not adoption proof, not final validation)
```

Claim ceiling for the RUN_002 Arm B layer: candidate validation and a cross-model replication signal
only — not adoption proof, not final validation, not standalone proof of adoption. `results/` remains
`.gitkeep`-only; no experiment results have been promoted.

PR-N — v0.5.0 release preparation (this PR; documentation and owner-review aids only, no release action):

```text
releases/v0.5.0_RELEASE_NOTES_DRAFT.md                          this file, caught up to PR #100-#113
releases/v0.5.0_COVERAGE_PROPOSAL_PREVIEW_FOR_OWNER_REVIEW.json coverage proposal preview (not a manifest;
                                                                per-file SHA-256; pinned commit)
releases/RELEASE_EXECUTION_v0_5_0.md                            owner execution runbook aligned to
                                                                docs/RELEASE_PROCESS.md (asset-only
                                                                manifest location)
```

PR-N performs no release action: no tag, no signing, no signed manifest, no trust-anchor entry, no
GitHub Release. Those remain human-owner actions under docs/RELEASE_PROCESS.md.

---

## Boundaries preserved

```text
No adoption verdicts: all experiment outputs are candidate signals requiring human review.
No claim that any AI system has adopted this canon.
experiment_artifact manifests cannot enter the release signing flow.
Release verification for current_release and retroactive_reconstruction stays whole-repository.
v0.4.1 trust anchor unchanged.
provenance/manifests/ remains retroactive_reconstruction only.
No license selected: docs/LICENSE_POLICY_DRAFT.md is NOT A LICENSE and grants no rights.
```

---

## Release checklist (human-owner actions, not performed yet)

```text
1. Release-candidate audit of main.
2. Tag the release commit.
3. Build the v0.5.0 current_release manifest (GitHub Release asset only, never committed).
4. Sign with the user-held origin key per docs/RELEASE_PROCESS.md.
5. Publish release assets and update docs/TRUST_ANCHOR.md.
6. Replace this draft with final release notes.
```

AI agents, CI, and automation do not perform any step of this checklist.

End of file.
